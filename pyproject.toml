[project]
name = "iot-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[tool.uv.workspace]
members = [
    "iot-visuals/server_code",
    "iot-visuals",
    "tests",
]
